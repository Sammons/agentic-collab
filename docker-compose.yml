services:
  orchestrator:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - orchestrator-data:/data/.agentic-collab
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

volumes:
  orchestrator-data:
