{
  "name": "agentic-collab",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "orchestrator": "node src/orchestrator/main.ts",
    "proxy": "node src/proxy/main.ts",
    "test": "node --test 'src/**/*.test.ts'",
    "test:watch": "node --test --watch 'src/**/*.test.ts'",
    "typecheck": "tsc --noEmit"
  }
}
